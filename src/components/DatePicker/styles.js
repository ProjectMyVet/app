import { StyleSheet } from 'react-native'
import { COLORS } from '../../constants'

export default StyleSheet.create({
    datePickerLabel: {
        fontSize: 16,
        color: COLORS.BLACK,
    },
    datePickerStyle: {
        width: '100%',
        color: COLORS.BLACK,
        marginBottom: 10
    }
})
